import sqlite3

conn = sqlite3.connect('test.db')

cursor = conn.execute("SELECT id, attack_line from attacks")

print "Opened database Successfully";


for row in cursor:
    print "id = ", row[0]
    print "attack_line = ", row[1]

print "Operation done successfully";

conn.close() 
